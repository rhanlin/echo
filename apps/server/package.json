{
  "name": "@echo/server",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@echo/envelope": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cfworker/json-schema": "^4.0.0"
  }
}
